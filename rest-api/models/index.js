const userModel = require('./userModel');
const tokenBlacklistModel = require('./tokenBlacklistModel');
const favouriteMovieModel = require('./favouriteMovieModel');

module.exports = {
    userModel,
    tokenBlacklistModel,
    favouriteMovieModel,
}